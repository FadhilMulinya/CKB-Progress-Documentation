# HELLO WORLD
1. Wrote a gift card contract in Rust

2. Compiled it to RISC-V binary

3. Tested it locally 

4. Deployed it to CKB testnet

Anyone can create gift card cells that use your contract's code_hash as their type script — and the rules written (claim after block, preserve message) will be enforced by the CKB-VM.

